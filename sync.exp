#!/usr/bin/expect -f

set timeout -1
spawn rsync -avz -e "ssh -o StrictHostKeyChecking=no" --exclude ".git" --exclude "node_modules" --exclude "target" --exclude "dist" --exclude ".next" --exclude ".idea" --exclude ".vscode" --exclude "github_deploy_key*" . root@54.255.186.244:/root/project-manage

expect {
  "password:" {
    send "NerssBiU56\r"
    exp_continue
  }
  eof
}

wait
