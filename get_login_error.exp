#!/usr/bin/expect -f
set timeout -1
spawn ssh -o StrictHostKeyChecking=no root@54.255.186.244
expect {
  "password:" {
    send "NerssBiU56\r"
    exp_continue
  }
  "#" {
    send "docker logs --tail 100 project-manage-backend > /root/login_err.log && cat /root/login_err.log\r"
  }
}
expect "#"
send "exit\r"
expect eof
