#!/usr/bin/expect -f
set timeout -1
spawn ssh -o StrictHostKeyChecking=no root@54.255.186.244 "cd /root/project-manage && git remote prune origin && git fetch --all --prune && git reset --hard origin/main"
expect {
  "password:" {
    send "NerssBiU56\r"
    exp_continue
  }
  eof
}
