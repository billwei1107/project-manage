# 2026-02-27 開發日誌

## 開發規劃 (Planning)
- 實作「專案待辦事項同步至行事曆」功能，將原本分散各專案的任務在單一月曆檢視中呈現。
- 以顏色區分一般事件與專案待辦事項。
- 部署至測試伺服器供客戶檢視。

## 專案結構 (Project Structure)
```
.
├── README.md
├── backend/            # Spring Boot Backend
├── deploy.exp          # Deployment script
├── devlog/             # Development logs
├── docker/             # Docker configurations
├── docs/               # Documentation
├── env/                # Environment variables
├── frontend-app/       # Flutter Frontend (Mobile)
├── frontend-web/       # React Frontend (Web)
└── startup_error.log   # Error log
```

## 進度 (Progress)
- **同步專案待辦事項至行事曆**：
  - 更新後端 `TaskRepository` 與 `TaskService`，支援根據當前使用者的權限，跨專案過濾出參與專案的 `Task`。
  - 在 `TaskController` 暴露新的安全端點 `/api/v1/tasks/my-calendar-tasks` 供前端存取。
  - 前端 Zustand (`useEventStore.ts`) 同步呼叫事件與專案任務的 API 並統一管理。
  - 調整 `Calendar.tsx` 整合兩種不同類型的資料結構，使用醒目的綠色區塊來呈現「專案待辦事項」，並修改 `Tooltip` 提供有意義的提示文字。

## Git 提交記錄

- **提交時間**: 12:45
- **提交 Hash**: `1c69309`
- **提交訊息**: `feat(calendar): sync project tasks to unified calendar timeline`
  - 新增 `TaskService.getMyCalendarTasks` 與對應 API。
  - 修改 `useEventStore` 平行獲取 tasks 與 events。
  - 統一映射資料結構供 `Calendar.tsx` 渲染，並補上型別安全與不同顏色的排版區隔。
