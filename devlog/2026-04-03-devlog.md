# 2026-04-03 開發日誌

## 開發規劃 (Planning)
- [x] 修復後端 JWT Filter 在處理請求時因為憑證過期拋出 `ExpiredJwtException` 導致登入 API 回傳 403 的問題。

## 專案結構 (Project Structure)
(維持原樣)

## 進度 (Progress)
- 修復 `JwtAuthenticationFilter` 對過期 JWT 拋出 Exception 的問題。
- 修復 `AuthController` 對 `BadCredentialsException` 與 `NoSuchElementException` 拋出 403 的問題，改為回傳 401 狀態碼與正確的錯誤訊息給前端。
- 修復 `DataSeeder.java` 中預設管理員沒有 `username` 的問題，避免輸入 `admin` 無法登入。

## Git 提交記錄
- **提交時間**: 19:50
- **提交 Hash**: `67eb145`
- **提交訊息**: `fix(auth): handle expired jwt exception in filter to prevent login failure`
  - 擷取 JWT 出現 Exception 時，不再直接中斷，而是拋棄憑證以非登入狀態進入 SecurityFilterChain 驗證。
- **變更統計**: 1 個檔案 (+8/-2)
