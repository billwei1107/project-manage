import { Outlet } from 'react-router-dom';

/**
 * @file index.tsx
 * @description employees page entry wrapper
 * @description_en wrapper for employee subroutes if any
 */
export default function EmployeesEntry() {
    return <Outlet />;
}
