#!/usr/bin/expect -f
set timeout -1
spawn ssh -o StrictHostKeyChecking=no root@54.255.186.244 "docker rm -f \$(docker ps -aq) && cd /root/project-manage && docker-compose -f docker/compose.yaml up -d --build"
expect "password:"
send "NerssBiU56\r"
expect eof
