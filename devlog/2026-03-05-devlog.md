## 2026-03-05 資訊門戶 (Information Portal) 開發
- 完成 `InfoPortal` 模組的後端 Database Entities 與 Repository 架構。
- 實作了 Spring Boot REST API (`ClientController`, `DirectoryController`, `FileEntityController`, `ShareLinkController`, `AnnouncementController`)，以及對應的五個 Service，支援檔案分片儲存於本地 `$UPLOAD_DIR/info-portal`。
- 於 `SecurityConfig` 針對 `share-links/public/**` 解開免登入 JWT 限制，讓外部客戶上傳。
- 完成 React Frontend 的 `useInfoPortalStore` Zustand 狀態管理，負責拉取目錄、管理歷史導覽麵包屑與暫存上傳狀態。
- 實作 `ClientFileExplorer` 主操作介面，支援多層次資料夾瀏覽、拖曳/上傳、實作「建立分享連結」等功能。
- 實作 `AnnouncementList` 系統公告介面。
- 實作 `PublicUploadPortal` 外部上傳入口介面。
