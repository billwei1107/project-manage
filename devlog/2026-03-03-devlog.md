# 2026-03-03 開發日誌

## 開發規劃
- 修復專案移除成員時，因為 API 請求缺少欄位導致資料庫拋出 `Not Null Constraint` 的錯誤 (預算 `budget` 等必填欄位被清為 Null)。
- 完善 GitHub 自動化整合，確保從 ERP 專案中移除成員時，同步移除該成員對於對應 GitHub 倉庫的訪問權限。

## 專案結構
(結構無重大變動)

## 進度
- [x] 後端: 將 `ProjectService.updateProject` 內的實體更新邏輯從全欄位覆蓋改為部分更新 (Partial Update)，避免未傳遞的欄位被清空為 null 導致報錯。
- [x] 後端: 完善 `syncGithubCollaboratorRemovals` 方法，透過 GitHub API 自專案對應的 GitHub 倉庫中移出指定的協作者。
- [x] 後端: 將已綁定的 `githubUsername` 正確 Mapping 至 `ProjectResponse.MemberInfo` DTO 中回傳。
- [x] 前端: 在專案的成員列表中，顯示該用戶所關聯綁定的 GitHub 帳號，以提高識別度與除錯效率。

## Git 提交記錄
- **提交時間**: 09:30
- **提交 Hash**: `fffb4b0`
- **提交訊息**: `fix(backend): project update should be partial instead of complete replacement to prevent clearing fields to null`
  - 修復移除專案成員時，由於前端只發送 `teamIds` 負載，導致後端將其他必填欄位覆蓋洗空為 null 的嚴重例外錯誤。
  - 將專案更新邏輯改為「必須加上 Null 判斷」的安全部分更新。

## 測試反饋記錄
- 移除專案成員後，成功觸發 `syncGithubCollaboratorRemovals` 並自 GitHub 倉庫移除該成員權限，且資料庫不再拋出 Non-Null 違反的 Exception。
