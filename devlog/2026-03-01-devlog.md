# 2026-03-01 開發日誌

## 開發規劃 (Planning)
- 整合 GitHub Organization 功能，支援在專案建立時自動建立 GitHub 倉庫。
- 實作團隊成員自動同步至 GitHub Collaborators 邏輯。
- 在專案詳情頁面新增「Sync to GitHub」手動同步按鈕。
- 修正前端 TypeScript 型別錯誤與 Alert 元件引用問題。

## 專案結構 (Project Structure)
```
/Users/billwei1107/Desktop/code/project manage/
.
├── .github/
│   └── workflows/
│       ├── check-ports.yml
│       └── deploy.yml
├── backend/
│   ├── src/
│   │   └── main/
│   │       ├── java/com/erp/
│   │       │   ├── controller/
│   │       │   ├── dto/
│   │       │   ├── entity/
│   │       │   ├── repository/
│   │       │   └── service/
│   │       └── resources/
│   │           └── application.yml
├── devlog/
├── docker/
├── docs/
├── frontend-web/
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   ├── pages/
│   │   └── stores/
└── manual_deploy.exp
```

## 進度 (Progress)
- **GitHub 整合功能實作**：
  - **後端**：
    - `User.java`: 新增 `githubUsername` 欄位。
    - `GitHubService.java`: 新增 `createOrganizationRepository` 與 `addCollaboratorToRepo` 方法。
    - `ProjectService.java`: 實作建立專案時自動建立 GitHub 倉庫與同步成員邏輯。
    - `ProjectController.java`: 新增 `/api/v1/projects/{id}/github/sync-members` 接口供手動同步。
    - `application.yml`: 配置 `github.org.name` 與 `github.org.token`。
  - **前端**：
    - `AddProjectModal.tsx`: 加入「建立 GitHub 倉庫」與「私人倉庫」切換開關。
    - `ProjectDetail.tsx`: 團隊區塊新增 `Sync to GitHub` 按鈕，串接同步成員 API。
    - `projects.ts` (API): 加入 `syncGithubMembers` 與相關型別定義。
    - 修正 `AddProjectModal.tsx` 中的 `Alert` 引用缺失與重覆 `Divider` 等 lint 錯誤。

## Git 提交記錄

- **提交時間**: 13:30
- **提交 Hash**: `a438cef`
- **提交訊息**: `chore(github): remove unused import in GitHubService`
  - 最終代碼清理，移除未使用的引用。
  - 同步推送至測試伺服器進行部署。
