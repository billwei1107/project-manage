# 開發日誌 - 2026-03-30

## 1. 開發規劃 (Planning)
- [x] **專案管理 (Projects)**：根據 Figma 實作「Projects - Timeline」甘特圖時程檢視模式。

## 2. 專案結構 (Project Structure)
目前核心新增修改組件：
```
frontend-web/src/
├── components/projects/
│   ├── TaskRowCard.tsx         // 列表專用任務卡 (List)
│   ├── TaskKanbanCard.tsx      // 看板專用任務卡 (Board)
│   ├── DroppableKanbanColumn.tsx  // 看板拖曳置放區 (Board)
│   └── TaskTimelineBoard.tsx   // ✨【新增】甘特圖時程表 (Timeline)
└── pages/projects/
    └── ProjectList.tsx         // ✨【修改】新增 Timeline 切換模式並修改 Task 資料屬性
```

## 3. 進度與變更 (Progress)
- **UI 開發**：
  - 新增 `TaskTimelineBoard.tsx` 元件，重現了 Figma 裡的 30 天排程方塊表。使用了 `position: sticky` 來實現左側任務名稱固定而右側甘特區域可靈活水平滑動 (Horizontal scroll) 的行為。
  - 方塊著色：空白日程背景使用 `#F4F9FD`，活躍狀態或排程日期區間則使用淺藍色 `#A7CAFF` 呈現。
  - 實作了 Hover Timeline 方塊時顯示 `Assignee` (包含大頭貼與名字) 的懸浮氣泡 Tooltip，完全還原 Figma 指示的白底加陰影與向下小箭頭視圖。
  - **新功能**：新增了過濾條件側拉選單 (`TaskFilterDrawer.tsx`)。透過右側滑入的動態 Drawer 呈現豐富的表單元件，包括自訂的複選框 (Task Group, Reporter)、大頭貼與人名晶片 Chips (Assignees) 以及底部的保存按鈕操作區，還原 Figma "Projects - Board - filters" 以利未來進行條件查詢與列表篩選。
  - **新頁面**：新增了任務明細頁面 (`TaskDetails.tsx`)。根據 Figma "Projects - Project and task details - in review" 實作了 3 欄式設計。
    - 左欄 (Project Info)：包含專案編號、描述、Reporter、Assignees 頭貼群組、優先級與截止日，底部包含附檔與連結快速操作按鈕。
    - 中欄 (Task Details)：包含任務詳情、可互動狀態選單 (Status Dropdown, 包含 To Do, In Progress, In Review, Done 即時顏色切換)、附件清單 (Attachment previews) 以及近期活動 (Recent Activity) Timeline 面板。
    - 右欄 (Task Info)：包含負責人與分派人、圓形進度圖表 (Time tracking) 與紀錄時間按鈕。
  - **新彈窗組件**：新增 `ApproveTaskModal.tsx`，當使用者從下拉選單選擇 `Done` 狀態時會自動彈出。包含生成的插圖、確認文字，以及負責處理進度轉換至已完成的 `Approve Task` 主按鈕。
- **整合與路由重構**：
  - 在 `ProjectList.tsx` 導入包含圖標 (如 `FormatLineSpacingIcon`) 的模式切換器 (`viewMode: 'list' | 'board' | 'timeline'`)。
  - 串接 `FilterAltIcon` 按鈕，點擊後會彈出 TaskFilterDrawer 側拉選單。
  - 在 `App.tsx` 註冊了新的 `/admin/projects/task-details` 路由。
  - 修正所有使用 MUI (Grid v2) 及 Icon 的依賴庫衝突與引用過時問題，保證 TypeScript `noEmit` 檢查通過。

## Git 提交記錄
- **提交時間**: 10:40
- **提交 Hash**: `d0a1d81`
- **提交訊息**: `feat(ui): implement timeline gantt view for project tasks`
  - 根據 "Projects - Timeline" Figma 截圖設計新增了 `TaskTimelineBoard.tsx`。
  - 在 `ProjectList.tsx` 右上角的檢視清單增加 Timeline Icon Button。
  - 給任務加入 `startDay` 與 `duration` 以描繪 30 天行事曆的淡藍色 `#A7CAFF` 直塊。
- **變更統計**: 5 個檔案 (+60)

- **提交時間**: 14:47
- **提交 Hash**: `d0a1d82`
- **提交訊息**: `feat(frontend): implement collapsible task groups and update TaskRowCard UI to match Figma`
  - 在 `TaskRowCard.tsx` 中將標籤改為英文，修正 Priority 圖標與顏色定義，並實作空心圓勾選框。
  - 在 `ProjectDetail.tsx` 的 List 視圖中，透過 `Collapse` 元件實作 `Active Tasks` 底下的群組 (例如 Design) 折疊功能。
- **變更統計**: 2 個檔案 (+73/-29)

- **提交時間**: 14:52
- **提交 Hash**: `c711bac`
- **提交訊息**: `feat(frontend): implement LogTimeModal and integrate into TaskDetails`
  - 新增 `LogTimeModal.tsx`，實作了包含雙甜甜圈進度條造型的 Badge 橫幅及包含 Date、Time 與 Work Description 的填寫表單。
  - 於 `TaskDetails.tsx` 綁定「Log time」按鈕的 onClick 事件以觸發展示 `LogTimeModal` 彈出視窗。
- **變更統計**: 2 個檔案 (+206)
